import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Lotify</title>
      </Head>

      <main>
        <h1>Lotify</h1>
      </main>
    </div>
  );
};

export default Home;
